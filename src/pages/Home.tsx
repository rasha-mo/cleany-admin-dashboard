import React from 'react';

const Home: React.FC = () => {
    return (
        <div className="dashboard">
            <h1>Dashboard</h1>
            <p>Track high-level metrics, review recent activity, and monitor performance here.</p>
        </div>
    );
};

export default Home;